# Copy this to .env.local and fill values (do NOT commit real keys)

# OpenAI API key (server-side)
OPENAI_API_KEY=sk-...

# GitHub token with repo scope for private repos and issues (use the least privileges required)
GITHUB_TOKEN=ghp_...

# Optional: any provider or webhook secrets
# WEBHOOK_SECRET=...

# Note: this repo ignores .env* by default. Never commit secrets to the repository.
